
Puedes calcular el mismo valor poniendo el operador antes de =
x *= 2
x +=2

Para hacer operaciones dentro de un string puedes usar comillas invertidas y los operadores o variables entre `${ 2+3}`)

typeof se utiliza para saber el tipo de una variable

operadores booleanos true or false != ==

operadores logicos 
Solo admite tres, 
El operador AND (&&) , el operador OR ( || ) negacion (!)

Valores Vacios - Empty Values
Operador ternario
Condicional ? 1 : 2
Condicional ? 1 : 2

5>3 ? Si : no =si
1>2 ? si : no = NO

Function
function nombre(parametro)
{ lo que hace la funcion}

Arrow functions 
=> 
funcionhola = x => {x + 1}

cuando solo pasas un parametro no es necesario los parentesis 
Si no estas pasando parametros o si estas pasando 2 o mas parametros debes siempre usar parentesis